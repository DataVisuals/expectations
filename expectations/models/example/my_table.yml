version: 2
models:
- name: my_table
  description: Synthesised rules from DQ rule builder
  config:
    materialized: 'true'
  columns:
  - name: account
    tests:
    - test: dbt_expectations.expect_column_to_exist
